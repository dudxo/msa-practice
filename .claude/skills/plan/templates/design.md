# 설계 문서: (기능명)

> (예시) 설계 문서: 예약 주문 취소 규칙 추가

## 1. 메타 정보

> (예시)
> - 작성일: 2025-12-05
> - 도메인: Orders
> - 관련 이슈/기획:
>   - Slack: https://slack.com/...
>   - Notion: https://notion.so/...

- 작성일: YYYY-MM-DD
- 도메인: {Domain}
- 관련 이슈/기획:
  - (링크 또는 설명)

---

## 2. 배경 (왜 이 기능이 필요한가?)

> (예시)
> - 예약 주문 기능 도입 이후, 예약 주문도 일반 주문과 동일한 취소 제약을 받아 사용자 CS가 증가
> - 예약 주문은 배송 전에 언제든 취소 가능해야 한다는 기획 요구

- (기능의 목적과 배경)
- (사용자/운영/비즈니스 관점에서의 필요성)

---

## 3. To-do 리스트

> (예시)
> - [ ] 1. `OrdersService.cancelOrder` 메서드에 예약 주문 분기 추가
>   - 영향 파일: `src/orders/orders.service.ts`
> - [ ] 2. 예약 주문 취소 가능 조건 검증 로직 추가
>   - 조건: `reservedAt != null && status < SHIPPED`
> - [ ] 3. API 문서(Swagger) description 업데이트
>   - 영향 파일: `openapi.json`
> - [ ] 4. feature.md 규칙 섹션 업데이트
>   - 영향 파일: `docs/features/orders/feature.md`

- [ ] 1. (작업 내용)
  - 영향 파일: `src/{path}`
- [ ] 2. (작업 내용)
  - 영향 파일: `src/{path}`

---

## 4. API 설계서

> 설계 변경이 필요 없는 경우 (버그 수정, 단순 리팩토링 등):
> "설계 변경 없음. 기존 API/DB 구조 변경 없이 로직 수정만으로 해결 가능" 으로 대체

### 4.1 (기능명)

> (예시)
> #### `POST /api/v1/orders/:orderId/cancel`
>
> **설명**: 주문을 취소합니다.
>
> **요청**
> | 파라미터 | 타입 | 필수 | 설명 |
> |---------|------|-----|------|
> | orderId | string (path) | O | 주문 ID |
> | reason | string (body) | X | 취소 사유 |
>
> **응답 (200 OK)**
> ```json
> {
>   "orderId": "order_123",
>   "status": "CANCELLED",
>   "cancelledAt": "2024-01-15T10:30:00Z"
> }
> ```
>
> **에러 케이스**
> | 코드 | 상황 |
> |-----|-----|
> | 400 | 잘못된 요청 (유효하지 않은 orderId) |
> | 404 | 주문을 찾을 수 없음 |
> | 409 | 이미 취소된 주문 |

#### `METHOD /api/v1/{endpoint}`

**설명**: (설명)

**요청**
| 파라미터 | 타입 | 필수 | 설명 |
|---------|------|-----|------|
| (name) | (type) | O/X | (설명) |

**응답 (200 OK)**
```json
{
}
```

**에러 케이스**
| 코드 | 상황 |
|-----|-----|
| 400 | (상황) |
| 404 | (상황) |

---

## 5. DB 설계서

> 스키마 변경이 없는 경우: "스키마 변경 없음" 으로 대체

> (예시)
> ### 5.1 테이블: `orders`
> | 컬럼 | 타입 | 변경 | 설명 |
> |-----|------|-----|------|
> | cancel_type | VARCHAR(20) | **추가** | 취소 유형 (NORMAL, RESERVED) |
>
> ### 5.2 인덱스
> - `idx_orders_cancel_type` on `orders(cancel_type)` — 취소 유형별 조회 최적화

### 5.1 테이블: `{table_name}`

| 컬럼 | 타입 | 변경 | 설명 |
|-----|------|-----|------|
| (name) | (type) | 추가/수정/삭제 | (설명) |

### 5.2 인덱스

- (인덱스 정의 및 목적)

### 5.3 관계

- (테이블 간 관계 설명)

---

## 6. 테스트 관점

> (예시)
> | ID | 유형 | 시나리오 | 비고 |
> |---|------|---------|------|
> | T-CANCEL-001 | unit | 일반 주문, status=PAID → 취소 성공 | 기존 유지 |
> | T-CANCEL-002 | unit | 일반 주문, status=SHIPPED → 취소 실패 | 기존 유지 |
> | T-CANCEL-003 | unit | 예약 주문, 배송 전 → 취소 성공 | **신규** |
> | T-CANCEL-004 | unit | 예약 주문, 배송 후 → 취소 실패 | **신규** |

| ID | 유형 | 시나리오 | 비고 |
|---|------|---------|------|
| T-XXX-001 | unit | (시나리오) | 기존 유지 / **신규** |
